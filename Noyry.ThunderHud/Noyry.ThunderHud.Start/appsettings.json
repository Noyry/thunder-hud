{
  "Logging": {
    "LogLevel": {
      /*"Default": "Information"*/
      "Default": "Warning"
    }
  },

  "GameReaderApp": {
    /*I use resolved address instead of localhost because of int takes 2+ seconds .net HttpClient to resolve address for some reason*/
    /*Probably it's because IPv6 address is resolved first and the client have to wait response timeout before IPv4 address*/
    "GameInfoSourceAddress": "http://127.0.0.1:8111",
    "IndicatorsSubAddress": "indicators",
    "StateSubAddress": "state"
  }
}